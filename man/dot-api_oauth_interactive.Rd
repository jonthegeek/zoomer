% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_auth_shared.R
\name{.api_oauth_interactive}
\alias{.api_oauth_interactive}
\title{Interactive OAuth authorization-code flow via local server}
\usage{
.api_oauth_interactive(client, scopes, cache_key = NULL)
}
\arguments{
\item{client}{A Zoom OAuth client created with \code{\link[=zoom_client]{zoom_client()}}.}

\item{scopes}{A character vector of allowed scopes, such as "recording:read".}

\item{cache_key}{If you are authenticating with multiple users using the same
client, use this key to differentiate between those users.}
}
\value{
An \code{\link[httr2:oauth_token]{httr2::oauth_token()}}, invisibly.
}
\description{
Starts a local HTTP server (via httpuv) to capture the OAuth redirect,
opens the browser for the user to authorize, and exchanges the returned code
for a token. The token is cached in-memory.
}
\details{
Zoom does not permit localhost redirect URIs, so a publicly reachable relay
(e.g. a Cloudflare Worker) must be registered as the redirect URL in the
Zoom app config. The relay reads the local port from the \code{state} parameter
(format \code{"{port}:{nonce}"}) and issues a 302 to
\verb{http://localhost:\{port\}/callback}, where httpuv captures the code.
}
\keyword{internal}
