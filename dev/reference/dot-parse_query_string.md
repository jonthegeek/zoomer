# Parse a URL query string into a named list

Parse a URL query string into a named list

## Usage

``` r
.parse_query_string(qs)
```

## Arguments

- qs:

  A raw query string (with or without a leading `?`).

## Value

A named list of decoded key-value pairs.
