# Is a token expired?

Is a token expired?

## Usage

``` r
.api_token_is_expired(token)
```

## Arguments

- token:

  An Zoom API
  [`httr2::oauth_token()`](https://httr2.r-lib.org/reference/oauth_token.html).

## Value

A logical scalar indicating whether the token is expired.
