# Visit an OAuth client management page

Visit an OAuth client management page

## Usage

``` r
.api_browse_oauth_app(app_mgmt_url)
```

## Arguments

- app_mgmt_url:

  The page where the client can be managed.

## Value

The `app_mgmt_url`, invisibly.
